{{- if and .Values.global.zipkin .Values.global.zipkin.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.global.zipkin.name }}
  labels:
    app: {{ .Values.global.zipkin.name }}
    {{- include "banking-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.global.zipkin.service.type | default "ClusterIP" }}
  selector:
    app: {{ .Values.global.zipkin.name }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.global.zipkin.port }}
      targetPort: http
      {{- if eq (.Values.global.zipkin.service.type | default "ClusterIP") "NodePort" }}
      nodePort: {{ .Values.global.zipkin.service.nodePort }}
      {{- end }}
{{- end }}