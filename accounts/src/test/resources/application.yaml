spring:
  sql:
    init:
      mode: always
  cloud:
    consul:
      enabled: false
      config:
        enabled: false
        import-check:
          enabled: false
    discovery:
      enabled: false

  liquibase:
    enabled: true
    change-log: classpath:db.changelog/db.changelog-master.yaml